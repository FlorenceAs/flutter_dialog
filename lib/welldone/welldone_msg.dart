

import 'package:flutter/material.dart';
import 'package:flutterdialog/widgets/custom_dialog.dart';

class DialogWeldone {
   static exit(context) => showDialog(context: context, builder: (context) => DialogBox());
}